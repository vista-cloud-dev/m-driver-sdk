name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: vista-cloud-dev/.github/.github/workflows/go-ci.yml@main
    with:
      schema-check: false # library (package mdriver) — no `schema` command
  arch:
    uses: vista-cloud-dev/.github/.github/workflows/arch-waterline.yml@main
